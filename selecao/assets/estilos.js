import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
    fundo:{
        flex: 1,
        alignItems: 'center'
    }, 
    titulo: {
        fontSize: 18,
        color: 'white',
        fontWeight: 'bold',
        marginBottom: 40,
        marginTop: 20
    },
})

export default estilos