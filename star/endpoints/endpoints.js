const endpoint_principal = "https://www.googleapis.com/books/v1/volumes?q=intitle:star+wars&fields=items(id,volumeInfo(title,authors,pageCount,imageLinks(thumbnail)))&maxResults=20"

export default endpoint_principal